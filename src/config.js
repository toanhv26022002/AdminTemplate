const config = {
    url_backend: import.meta.env.VITE_URL_BACKEND,
    url_login: import.meta.env.VITE_URL_LOGIN,
    url_auth: import.meta.env.VITE_URL_AUTH,
    url_eofactory: import.meta.env.VITE_URL_EOFACTORY
};

export default config;
